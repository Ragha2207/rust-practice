// Struct Declaration
pub struct Color {
    pub red: u8,
    pub green: u8,
    pub blue: u8,
}

pub fn get_midnightblue_color() -> Color {
    Color {
        red: 25,
        green: 25,
        blue: 112,
    }
}
